name: Flutter CI

on:
  push:
    branches: [ SetupServer ]
  pull_request:
    branches: [ SetupServer ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - run: flutter pub get
      - run: flutter test --no-sound-null-safety
